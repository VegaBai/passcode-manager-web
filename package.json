{
  "name": "passcode-manager-web",
  "version": "1.0.0",
  "private": true,
  "description": "Badminton drop-in passcode pool and court queue manager for Vercel.",
  "scripts": {
    "dev": "node dev-server.js",
    "check": "node --check api/app.js && node --check app.js"
  },
  "dependencies": {
    "@libsql/client": "^0.17.0"
  },
  "engines": {
    "node": ">=20"
  }
}
